import React from 'react';

class Reactions extends React.Component{
    constructor(props) {
        super(props)
    }

    render() {
        return (
            <button></button>
        );
    }
};

export default Reactions;