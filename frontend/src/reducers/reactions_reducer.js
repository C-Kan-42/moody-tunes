// import { RECEIVE_REACTIONS } from '../actions/reaction_actions';

// const ReactionReducer = (
//     state = { all: {} }
// ) => {
//     Object.freeze(state);
//     let newState = Object.assign({}, state);

//     switch (action.type) {
//         case RECEIVE_REACTIONS:
//             newState.all = action.reactions;
//             return newState;
    
//         default:
//             return state;
//     }
// };

// export default ReactionReducer;